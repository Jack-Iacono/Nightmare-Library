using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyInfoScreenController : ScreenController
{
    // This will be used to hide info if the player hasn't already found it
    // ,but for now it just makes this object work alongside the UIController
}
